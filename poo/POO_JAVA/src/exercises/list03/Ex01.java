package exercises.list03;

public class Ex01 {
    /*
     01 - Explique com suas palavras quando utilizar cada um dos três modificadores
     de acesso em Java e também o que acontece com o nível de acesso quando não se aplica
     nenhum dos modificadores.

     R: No Java temos 4 modificadores de acesso:
      - Private: o modo mais restritivo, apenas a classe que criou tem acesso.
      - Default: quando não colocamos nenhum modificador de acesso ele fica como Default e com isso apenas as classes
                 que estão dentro do pacote tem acesso.
      - Protect: protect funciona tanto para as classes que criaram o atributo/método como também para as suas subclasses
                 ou as classes que estão dentro do mesmo pacote.
      - Public:  toda classe tem acesso, tando dentro como fora do pacote.
     */
}
